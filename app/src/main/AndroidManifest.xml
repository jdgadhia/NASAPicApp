<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.exam.nasapictureapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".NasaPicturesApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NASAPictureApp">
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.ImageDetailsActivity"
            android:label="@string/image_details"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.ActionBarNASAPictureApp" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/nasa_images"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.ActionBarNASAPictureApp" />

    </application>

</manifest>